package ReadDataExample;

import java.util.Scanner;

public class ReadData {
	/*
	 * public static Scanner input = new Scanner(System.in);
	 * 
	 * public static int readInt() {
	 * 
	 * boolean correct; boolean negative = false; String number;
	 * 
	 * do { correct = true; number = input.next(); if (number.charAt(0) == '-') {
	 * negative = true; number = number.substring(1, number.length()); }
	 * 
	 * for (int i = 0; i < number.length(); i++) { if
	 * (!Character.isDigit(number.charAt(i))) correct = false;
	 * System.out.print("input error, re-enter.: "); break; } } while (!correct);
	 * 
	 * if (negative) return -Integer.parseInt(number); return
	 * Integer.parseInt(number);
	 * 
	 * }
	 * 
	 * public static double readDouble() { boolean correct; boolean negative =
	 * false; String number; int decimalPlace; do { correct = true; number =
	 * input.next(); if (number.charAt(0) == '-') { negative = true; number =
	 * number.substring(1, number.length()); } decimalPlace = number.indexOf(".");
	 * 
	 * for (int i = 0; i < decimalPlace; i++) { if
	 * (!Character.isDigit(number.charAt(i))) { correct = false;
	 * System.out.print("input error, re-enter.: "); break; } } for (int i =
	 * decimalPlace + 1; i < number.length(); i++) { if
	 * (!Character.isDigit(number.charAt(i))) { correct = false;
	 * System.out.print("input error, re-enter.: "); break; } } } while (!correct);
	 * 
	 * if (negative) return -Double.parseDouble(number); return
	 * Double.parseDouble(number); }
	 */
}